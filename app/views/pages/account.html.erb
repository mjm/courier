<% content_for :script do %>
  <script type="text/javascript">
    window.elmFlags = Object.assign({},
      { stripeKey: "<%=j Rails.configuration.stripe[:publishable_key] %>" },
      { user: <%= raw @user_json %> }
    );
  </script>

  <%= javascript_pack_tag 'account' %>
<% end %>
